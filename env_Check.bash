#!/bin/bash


echo "===Try checking the values of these environment variables:=== "

echo ${HOSTNAME}
echo ${OSTYPE}
echo ${SHELL}
echo ${BASHPID}
echo ${BASH_VERSION}
echo ${UID}
echo ${GROUPS}

bash --version